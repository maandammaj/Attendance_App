# Lint Rules

Include `package:flutter_lints/flutter.yaml` in your `analysis_options.yaml`:

```yaml
include: package:flutter_lints/flutter.yaml

linter:
  rules:
    # Custom lint rules:
    # avoid_print: true
    # prefer_single_quotes: true