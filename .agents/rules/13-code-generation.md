# Code Generation

* **Build Runner:** Include `build_runner` as a dev dependency for projects with code generation.
* **Execution Command:** Run generation tasks using:

```bash
dart run build_runner build --delete-conflicting-outputs